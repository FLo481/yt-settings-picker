// Shared by the popup and the content script.
const DEFAULT_SETTINGS = {
  subtitlesOff: true,
  // Playback rate: "1", "1.5", "1.75" or "2".
  speed: "1",
  // "highest", or a YouTube quality name such as "hd1080".
  quality: "highest",
};
